import request, { extend, ResponseError, RequestError, fetch } from 'umi-request';
import zlrequest from './zlrequest';

export { fetch, extend, RequestError, ResponseError, zlrequest };

export default request;
